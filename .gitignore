# 编译产物（workspace 只在根目录产生一个 target/，这里写宽一点防止意外的嵌套 target）
/target
**/target

# 各 crate 目录下不应再单独出现 Cargo.lock（这是一个 workspace，统一用根目录的 Cargo.lock）
axum-static-embed/Cargo.lock
axum-static-embed-sample/Cargo.lock

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# 编辑器临时文件
*.swp
*.swo
*~
